<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.devMart.mapper.ReviewMapper">
    <select id="selectReviewListByUser" resultType="kr.co.devMart.common.util.CamelMap">
        SELECT * FROM TB_REVIEW WHERE username = #{username} ORDER BY reg_date DESC
    </select>
    <select id="selectReviewListByProduct" resultType="kr.co.devMart.common.util.CamelMap">
        SELECT * FROM TB_REVIEW WHERE product_id = #{productId} ORDER BY reg_date DESC
    </select>
    <insert id="insertReview">
        INSERT INTO TB_REVIEW (product_id, username, rating, content, reg_date)
        VALUES (#{productId}, #{username}, #{rating}, #{content}, NOW())
    </insert>
    <update id="updateReview">
        UPDATE TB_REVIEW SET rating=#{rating}, content=#{content}, upd_date=NOW()
        WHERE id=#{id} AND username=#{username}
    </update>
    <delete id="deleteReview">
        DELETE FROM TB_REVIEW WHERE id=#{id} AND username=#{username}
    </delete>
</mapper>
